class BinaryNode<T> {
  final T value;
  BinaryNode({required this.value});
}

List<num> binaryTree({required BinaryNode<num> binaryNode}) {
  //TODO: write binary tree
  throw UnimplementedError();
}
